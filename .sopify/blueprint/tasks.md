# 蓝图任务

## 未完成长期项
- [ ] 上架 Chrome 网上应用店（需稳定 ID、隐私说明、listing 文案）。
- [ ] 工具栏在任意页面打开同一 Side Panel（若第一版只从新标签页 FAB 进入，这里补齐）。

## 明确延后项
- [-] 多 CLI 选择器：第一版只接 `cursor-agent`。本机以后出现 Claude / Codex 且用户明确要选时再做。
- [-] `--mode ask` 之外的可写执行：用户明确要沙箱写盘后再做，默认仍不传 `--force`。
- [-] 微信读书、天气、音乐、番茄钟：没有新的日常使用证据前不做。
