# 蓝图任务

## 未完成长期项
- [ ] 上架 Chrome 网上应用店（需稳定 ID、隐私说明、listing 文案）。listing 须披露可选 `nativeMessaging`、本机 Host/CLI、任意页同一侧栏；不以聊天当主卖点，但不得把该能力写成未提供。

## 明确延后项
- [-] 接通 Claude / Codex 或其它 CLI：本方案只对接 Cursor。用户明确要选时另开方案。
- [-] `--mode ask` 之外的可写执行：用户明确要沙箱写盘后再做，默认仍不传 `--force`。
- [-] 微信读书、天气、音乐、番茄钟：没有新的日常使用证据前不做。
- [-] 标签跳转、去重、手动日夜切换：第一版按演示已有动作收口。
- [-] `chrome.storage.sync` 或把书桌数据写到 Host 文件：第一版只存在本机扩展存储。
