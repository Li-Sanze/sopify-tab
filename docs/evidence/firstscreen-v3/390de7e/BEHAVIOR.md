# 首屏 v3 行为记录（tip 390de7e）

拍法：Playwright Chromium headed，`--load-extension` 指向仓库 `extension/`。扩展 ID `cgkhllpelkjmfamddkjpnmchjikdcbgp`。页面是 `chrome-extension://…/newtab.html`，不是原型。

| 项 | 结果 | 说明 |
|---|---|---|
| 加载已解压的扩展 | 通过 | Playwright 等价于加载已解压。品牌 Chrome CLI 未用。 |
| 有数据首屏（白天） | 通过 | 「见 PR 评论」：待办标题、存窗「首屏改版」、随手记都在。 |
| 有数据首屏（夜晚淡星） | 通过 | `data-sky=night`，`.stars-layer` opacity 为 1。上半屏抽样到孤立亮点。白天同一层 opacity 为 0。 |
| 空态（无待办，白天） | 通过 | 占位「今天先做什么？」，接着上次为「还没有保存过」。 |
| 窗口几乎只有新标签 | 通过 | 1 个标签，「还没有可以保存的网页」，保存按钮 disabled。 |
| 视口 600 | 通过 | 「见 PR 评论」，宽 600。 |
| 默认关时不加载 three / embed | 通过 | 首次打开新标签页的请求里没有 `embed.js`、`scene.js`、`three.module.js`。 |
| 设置页不显示首屏星层 | 通过 | 切到设置后 `section.studio-desk` 为 hidden。本次是在白天切的，星层 opacity 本来就是 0。 |
| 双击「完成」只完成一条 | 通过 | 未完成 2 → 1。 |
| 随手记刷新仍在 | 通过 | 写入后 reload，文本还在。 |
| 改名：空名不写入 | 通过 | 清空后回车，名字仍是「首屏改版」。 |
| 改名：Esc 取消 | 通过 | 输入后 Esc，名字未改。 |
| 改名：回车保存 | 通过 | 回车后显示「实拍改名」。 |
| 改名：中文输入法选词 | 未验证 | 环境没有可用输入法。 |
| 改名：Tab 离开 / 点随手记且焦点不抢回 | 未验证 | 本轮只覆盖了空名、Esc、回车。 |
| 写入失败不显示「已存在本机」 | 未验证 | 无法让 `chrome.storage.local.set` 稳定失败。 |
| 第 6 次保存取消不写入 | 未验证 | 未把存窗堆到 5 份再点取消。 |
| 超过 50 页截断确认 | 未验证 | 未打开 51 个网页。 |
| 空间视图打开后三轮开关、中途关闭、隐藏页切回 | 未验证 | 只确认了默认关时不请求 three/embed。 |
| 宽度 900 / 390、完整 Tab 顺序、减少动态效果 | 未验证 | 只拍了 1280 与 600。 |
